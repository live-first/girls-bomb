export type MetaType = {
  totalCount: number
  offset: number
  limit: number
}

export type CommonType = {
  id: string
  createdAt: string
  updatedAt: string
  publishedAt: string
  revisedAt: string
}
